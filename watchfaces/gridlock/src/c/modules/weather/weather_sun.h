/**
 * @file weather_sun.h
 * @brief Two little panels. One for the sunrise time and one for the sunset time.
 * @ingroup gridlock_mod_weather
 */
#pragma once
#include "engine/catalog.h"

/** @brief The sunrise panel that the grid can drop in. */
extern const ModuleDef mod_weather_sunrise_def;

/** @brief The sunset panel that the grid can drop in. */
extern const ModuleDef mod_weather_sunset_def;
