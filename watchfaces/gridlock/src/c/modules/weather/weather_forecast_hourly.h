/**
 * @file weather_forecast_hourly.h
 * @brief The hourly forecast row. A full-width strip of upcoming hours, each with the time,
 * a sky icon and the temperature.
 * @ingroup gridlock_mod_weather
 */
#pragma once
#include <pebble.h>
#include "engine/catalog.h"

/** @brief The hourly forecast row the grid can drop in. */
extern const ModuleDef mod_weather_forecast_hourly_def;
