#pragma once
#include "engine/catalog.h"

/**
 * @file time_clock.h
 * @brief The little clock panel that shows the time as HH:MM with a clock icon.
 * @ingroup gridlock_mod_time
 */

/** @brief The clock panel hooked into the grid so the watchface can show it. */
extern const ModuleDef mod_time_clock_def;
