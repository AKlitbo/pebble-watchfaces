#pragma once
#include "engine/catalog.h"

/**
 * @file time_date.h
 * @brief The date panel that shows today's date and shrinks the text to make it fit.
 * @ingroup gridlock_mod_time
 */

/** @brief The date panel hooked into the grid so the watchface can show it. */
extern const ModuleDef mod_time_date_def;
