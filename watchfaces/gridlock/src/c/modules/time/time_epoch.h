/**
 * @file time_epoch.h
 * @brief A little panel showing the raw Unix time, ticking up every minute.
 * @ingroup gridlock_mod_time
 */
#pragma once
#include "engine/catalog.h"

/** @brief The epoch clock panel that the grid can drop in. */
extern const ModuleDef mod_time_epoch_def;
