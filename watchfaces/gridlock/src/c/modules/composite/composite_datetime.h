#pragma once
#include "draw/common.h"

/**
 * @file composite_datetime.h
 * @brief The full-width bar showing the time, the date beside it, and a Bluetooth icon
 * in the corner.
 * @ingroup gridlock_mod_composite
 */

/** @brief The composite datetime panel hooked into the grid so the watchface can show it. */
extern const ModuleDef mod_composite_datetime_def;
